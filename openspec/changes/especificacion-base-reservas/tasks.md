## 1. Rama de trabajo

- [x] 1.1 Crear la rama `feature/spec-contrato-base` desde `main`, llevando los cambios todavía sin commitear, y hacer ahí todos los commits de este cambio: formato convencional en castellano, desde la cuenta de cada integrante y sin trailers ni pies de atribución a agentes (`AGENTS.md`). Verificar que `git branch --show-current` devuelve `feature/spec-contrato-base` antes del primer commit.

## 2. Corregir y ampliar `docs/requisitos.md`

- [x] 2.1 Corregir el título (`l# Sistema de Reservas` → `# Sistema de Reservas`) y agregar debajo una nota: desde el archivado, el comportamiento vigente está en `openspec/specs/`, la forma de la API en `contratos/openapi.yaml` y la referencia visual en `docs/claude-design/`. Verificar que la vista previa de Markdown muestra el H1 y la nota.
- [x] 2.2 En §1, §2 y §3: reemplazar "canchas, tarifas y equipamiento" por "canchas con su precio por turno y equipamiento"; sumar al alcance del MVP el panel del club, la administración de canchas y equipamiento y el reenvío de mails; quitar **Tarifa** del glosario y redefinir **Disciplina** sin "cantidades de jugadores admitidas". En la tabla de permisos: "Ver catálogo y precios"; "ABM de canchas y equipamiento" solo ADMIN; filas nuevas "Ver panel del club" (solo ADMIN), "Reenviar mail de reserva propia" (SOCIO y ADMIN) y "Reenviar mail de reserva de terceros" (solo ADMIN). Verificar leyendo las tres secciones que no queda ninguna mención a tarifas y que la tabla coincide con las specs.
- [x] 2.3 En §4: marcar RN-11 y RN-12 como "Dada de baja: precio plano por turno", sin renumerar; cambiar RN-06 a "cambios de precio posteriores"; agregar **RN-15** (dar de baja una cancha o un equipamiento no modifica ni cancela las reservas existentes) y **RN-16** (una reserva admite como máximo 3 reenvíos de mail por hora); quitar la columna "Jugadores admitidos" de la tabla de disciplinas y reemplazar el párrafo del diferencial de precio por la regla de precio plano con `cantidadJugadores` opcional e informativo; sumar `PREFIJO_CODIGO_RESERVA` (`RES`) y `ZONA_HORARIA_CLUB` (`America/Argentina/Cordoba`) a la tabla de configuración. Verificar que la tabla de reglas lista RN-01 a RN-16 con RN-11 y RN-12 dadas de baja.
- [x] 2.4 En §5, RF-01 a RF-04: en RF-01, cambiar el tercer criterio por "incluye la duración de turno"; en RF-02, titularlo "Consultar canchas" y cambiar el tercer criterio por "incluye un único `precioPorTurno`"; en RF-04, reemplazar los criterios de RN-11 y RN-12 por "2 y 4 jugadores en la misma cancha dan el mismo monto de cancha" y "`cantidadJugadores: 3` en tenis se acepta", y el de RN-06 por "luego cambia el precio de la cancha". Verificar que cada criterio modificado tiene un escenario equivalente en `specs/catalogo/spec.md` o `specs/reservas/spec.md`.
- [x] 2.5 En §5, RF-05, RF-08 y RF-09: agregar a RF-05 que cada reserva incluye el nombre del titular (`cliente`); cambiar los asuntos de la tabla de RF-08 por `Tu turno en Deploy está confirmado · {codigo}` y `Cancelamos tu turno en Deploy · {codigo}`; reescribir las secciones mínimas de RF-09 como Inicio, El club y Contacto (dirección, teléfono, mail y horarios, sin redes) y referenciar el prototipo en lugar de `identidad.md`. Verificar que coinciden con `specs/reservas/spec.md`, `specs/notificaciones/spec.md` y `specs/institucional/spec.md`.
- [x] 2.6 Agregar en §5 **RF-11 Panel del club** (`GET /admin/panel`), **RF-12 Administración de canchas** (`POST /canchas`, `PATCH /canchas/{id}`, `incluirInactivas`), **RF-13 Administración de equipamiento** (`POST /equipamiento`, `PATCH /equipamiento/{id}`, `incluirInactivos`) y **RF-14 Reenvío del mail** (`POST /reservas/{id}/reenvio-mail`), cada uno con criterios `DADO / CUANDO / ENTONCES` equivalentes a los escenarios de `specs/administracion/spec.md`, de los requisitos de administración de `specs/catalogo/spec.md` y del requisito de reenvío de `specs/notificaciones/spec.md`. Verificar que cada escenario de esos requisitos tiene su criterio.
- [x] 2.7 En §6: quitar `tarifa` del diagrama y eliminar su tabla; quitar `jugadores_permitidos` de `disciplina`; agregar `precio_por_turno numeric(10,2)` a `cancha` y borrar "El precio ya no vive acá"; agregar `activo boolean default true` a `equipamiento` y `reenvio boolean default false` a `notificacion`; marcar `reserva.cantidad_jugadores` como nulable e informativa; documentar `hora_inicio` y `hora_fin` como `varchar(5)` en formato `HH:MM`, y `codigo` con prefijo configurable. Verificar que las tablas coinciden campo por campo con el `schema.prisma` de la tarea 7.1.
- [x] 2.8 En §7, quitar RN-11 de la fila 422, sumar "reenvío no disponible" a la fila 409 y agregar la fila 429 (límite de contacto y de reenvíos); en §8, dejar la fila de C como "RF-04 (RN-01, 05, 06)" y agregar una fila "RF-11 a RF-14, administración y reenvío — a asignar" con la rama `feature/spec-administracion`. Verificar con `rg -i "tarifa|RN-11|RN-12|jugadores_permitidos" docs/requisitos.md` que solo aparecen las dos filas dadas de baja de §4.

## 3. Ampliar `contratos/openapi.yaml` a 2.2.0

- [x] 3.1 Subir `info.version` a `2.2.0`, agregar el tag `Administracion` y actualizar la descripción de roles: `ADMIN` además administra canchas y equipamiento y ve el panel. Verificar que `npm run contrato:lint` termina con código 0.
- [x] 3.2 Canchas: agregar `POST /canchas` y `PATCH /canchas/{id}` con `CrearCanchaRequest` y `ActualizarCanchaRequest`, y el parámetro `incluirInactivas` en `GET /canchas` con respuestas 401 y 403, con la seguridad y los códigos de la tabla de la decisión 16. Verificar con `npm run contrato:lint` y comparando operación por operación contra esa tabla.
- [x] 3.3 Equipamiento: agregar `POST /equipamiento` y `PATCH /equipamiento/{id}` con `CrearEquipamientoRequest` y `ActualizarEquipamientoRequest`, el parámetro `incluirInactivos` en `GET /equipamiento` con respuestas 401 y 403, y el campo `activo` en `Equipamiento`. Verificar con `npm run contrato:lint` y contra la tabla de la decisión 16.
- [x] 3.4 Reservas y reenvío: agregar el campo `cliente` (string) a `Reserva` y `POST /reservas/{id}/reenvio-mail` con `ReenvioMailResponse` y respuestas 202, 401, 404, 409 (ejemplo `REENVIO_NO_DISPONIBLE`) y 429 (ejemplo `DEMASIADAS_SOLICITUDES`). Verificar con `npm run contrato:lint`.
- [x] 3.5 Panel: agregar `GET /admin/panel` con el parámetro opcional `fecha` y los schemas `PanelAdmin`, `OcupacionCancha` y `ProximoTurno` con los campos de la decisión 16, y respuestas 200, 400, 401 y 403. Verificar con `npm run contrato:lint` y generando tipos en un archivo temporal con `npx openapi-typescript contratos/openapi.yaml -o <carpeta temporal>/schema.d.ts`, que debe terminar sin errores e incluir `PanelAdmin`.

## 4. Arquitectura, identidad y prototipo

- [x] 4.1 Actualizar `docs/arquitectura.md`: en §1 dejar `docker-compose.yml` en el árbol con el comentario "PostgreSQL 17 local (puerto 5434)" y agregar `docs/claude-design/` y `docs/memoria-proyecto.md`; en §5 cambiar `DATABASE_URL` a `postgresql://club:club@localhost:5434/club_reservas` y aclarar que apunta al contenedor de `docker-compose.yml`; en §6 cambiar `image: postgres:16` por `image: postgres:17`; en §9 sumar la etapa de administración. Verificar que `rg -n "postgres:16" docs/arquitectura.md` no encuentra nada y que `rg -n "5432" docs/arquitectura.md` solo aparece en el workflow del CI.
- [x] 4.2 Agregar al inicio de `docs/identidad.md` una nota: el club se llama **Deploy** y la referencia visual vigente (colores, tipografías, pantallas) es `docs/claude-design/Deploy Club.dc.html`; lo que sigue queda como antecedente. Verificar que la nota es lo primero que se lee después del título.
- [x] 4.3 Sumar `docs/claude-design/uploads/` a `.gitignore`. Verificar que `git check-ignore "docs/claude-design/uploads/padel.png"` lo informa como ignorado y que `git status` muestra `Deploy Club.dc.html`, `support.js`, `.thumbnail` y `assets/` como pendientes de agregar.

## 5. PostgreSQL en Docker

- [x] 5.1 Crear `docker-compose.yml` en la raíz con el servicio de la decisión 13: imagen `postgres:17`, contenedor `club-db`, usuario, clave y base `club` / `club` / `club_reservas`, puerto `5434:5432`, volumen `db-data` y healthcheck con `pg_isready`. Verificar que `docker compose config` termina sin errores y muestra el puerto publicado 5434.
- [x] 5.2 Con Docker Desktop abierto, levantar la base con `docker compose up -d --wait` (el script `db:up` se ajusta en la tarea 6.3). Verificar que `docker ps` muestra `club-db` como `healthy`, que `docker compose exec db psql -U club -d club_reservas -c "SELECT version();"` informa PostgreSQL 17.x y que `docker compose exec db psql -U club -d club_reservas -c "SELECT rolsuper FROM pg_roles WHERE rolname = 'club';"` devuelve `t`, necesario para la base sombra de Prisma.

## 6. Dependencias y configuración de la API

- [x] 6.1 Instalar Prisma con versión exacta (`npm install --workspace api --save-exact @prisma/client@6.19.3` y `npm install --workspace api --save-exact -D prisma@6.19.3`) y bcrypt (`npm install --workspace api bcrypt` y `npm install --workspace api -D @types/bcrypt`), sin correr `prisma init`. Verificar que `apps/api/package.json` lista `prisma` y `@prisma/client` como `6.19.3` sin `^`, y que `npm exec --workspace api -- prisma --version` informa 6.19.3.
- [x] 6.2 Crear `apps/api/.env.example` con `DATABASE_URL=postgresql://club:club@localhost:5434/club_reservas`, que va con valor porque es la misma en todas las máquinas y no es secreta, y con las claves vacías `JWT_SECRET`, `JWT_EXPIRES_IN`, `RESEND_API_KEY`, `MAIL_FROM`, `FRONTEND_URL`, `CANCELACION_MINUTOS_MINIMOS`, `HORIZONTE_RESERVA_DIAS`, `MAX_RESERVAS_ACTIVAS_SOCIO`, `HORA_APERTURA`, `HORA_CIERRE`, `PREFIJO_CODIGO_RESERVA` y `ZONA_HORARIA_CLUB`. Crear también un `apps/api/.env` local con esa `DATABASE_URL`, los valores de desarrollo del paso 3.6, `PREFIJO_CODIGO_RESERVA=RES` y `ZONA_HORARIA_CLUB=America/Argentina/Cordoba`. Verificar que `git check-ignore apps/api/.env` lo informa como ignorado y que `git status` muestra `.env.example` pero no `.env`.
- [x] 6.3 En el `package.json` raíz: cambiar `db:migrate` a `npm exec --workspace api -- prisma migrate dev`, `db:seed` a `npm exec --workspace api -- prisma db seed` y `db:studio` a `npm exec --workspace api -- prisma studio`, cambiar `db:up` a `docker compose up -d --wait` y dejar `db:down` como `docker compose down` (decisiones 10 y 13). Verificar que `npm run db:down` y después `npm run db:up` dejan `club-db` como `healthy`. El resto se verifica en la tarea 7.2, que corre `npm run db:migrate` desde la raíz y debe mostrar que Prisma cargó `.env` y se conectó a `club_reservas` en `localhost:5434`.

## 7. Schema y migraciones

- [x] 7.1 Escribir `apps/api/prisma/schema.prisma` con el schema del paso 3.7 del plan y los cinco ajustes de la decisión 3: sin `@@index([canchaId, fecha, horaInicio])`; con `map: "ix_reserva_usuario_estado"` y `map: "ix_reserva_fecha"` en los dos índices de `Reserva`; `horaInicio` y `horaFin` como `VarChar(5)`; `Equipamiento.activo Boolean @default(true)`; y `Notificacion.reenvio Boolean @default(false)`. Verificar que `npm exec --workspace api -- prisma validate` termina sin errores.
- [x] 7.2 Generar la migración inicial con `npm run db:migrate -- --name init` desde la raíz. Verificar que existe `apps/api/prisma/migrations/<timestamp>_init/migration.sql` sin tabla `tarifa` ni columna `jugadores_permitidos`, con las columnas `equipamiento.activo` y `notificacion.reenvio`; que no aparece el error `P3014` de base sombra; y que `docker compose exec db psql -U club -d club_reservas -c "\dt"` lista `usuario`, `disciplina`, `cancha`, `equipamiento`, `reserva`, `reserva_equipamiento`, `notificacion` y `_prisma_migrations`.
- [x] 7.3 Crear la migración `slot_unico_activo` con `npm run db:migrate -- --create-only --name slot_unico_activo`, escribir en su `migration.sql` el `CREATE UNIQUE INDEX "ux_reserva_slot_activo" ... WHERE "estado" <> 'CANCELADA'` de la decisión 4 y aplicarla con `npm run db:migrate`. Verificar con `SELECT indexname, indexdef FROM pg_indexes WHERE tablename = 'reserva';` que existen `ux_reserva_slot_activo` (con la cláusula `WHERE`), `ix_reserva_usuario_estado` e `ix_reserva_fecha`.
- [x] 7.4 Comprobar que Prisma no intenta borrar el índice parcial: correr `npm run db:migrate -- --create-only --name verificacion`. Si informa que el schema ya está sincronizado, no hay nada que hacer. Si genera una migración con `DROP INDEX "ux_reserva_slot_activo"`, borrar esa carpeta y anotar en la descripción del PR que toda migración futura se revisa buscando ese `DROP`. Verificar que en `apps/api/prisma/migrations/` quedan solo `init` y `slot_unico_activo`.
- [x] 7.5 Generar el cliente con `npm exec --workspace api -- prisma generate`. Verificar que `npm run build --workspace api` compila sin errores con TypeScript 6.

## 8. Datos de prueba

- [x] 8.1 Escribir `apps/api/prisma/seed.ts` con los datos de la tabla de la decisión 11 (3 disciplinas, 6 canchas, 5 ítems de equipamiento, con "Tubo de pelotas" separado para tenis y para pádel) y los dos usuarios de prueba, más la guardia: si ya existe alguna disciplina, informar que la base tiene datos y terminar con código 0. Agregar `"prisma": { "seed": "ts-node prisma/seed.ts" }` en `apps/api/package.json`. Si `ts-node` no ejecuta con TypeScript 6, probar `ts-node --transpile-only prisma/seed.ts`; si tampoco funciona, frenar y actualizar la propuesta para sumar `tsx`. Verificar que `npm run db:seed` desde la raíz imprime "Datos de prueba cargados." y que la base tiene 3 disciplinas, 6 canchas activas con los precios de la tabla, 5 ítems de equipamiento activos con disciplina asignada y 2 usuarios con `password_hash` que empieza con `$2b$`.
- [x] 8.2 Verificar la idempotencia: una segunda ejecución de `npm run db:seed` termina con código 0, informa que la base ya tiene datos y los conteos no cambian; `npm exec --workspace api -- prisma migrate reset --force` recrea la base y vuelve a dejar los mismos conteos.

## 9. Verificación integral y cierre

- [x] 9.1 Prueba de humo de RN-01 con `docker compose exec db psql -U club -d club_reservas`, dentro de `BEGIN; ... ROLLBACK;`: insertar dos reservas `CONFIRMADA` para la misma cancha, fecha y `hora_inicio` debe fallar con `duplicate key value violates unique constraint "ux_reserva_slot_activo"`; en otra transacción, una reserva `CANCELADA` y una `CONFIRMADA` para el mismo turno deben insertarse las dos. Verificar después que `SELECT count(*) FROM reserva;` devuelve 0.
- [x] 9.2 Simular el CI: crear `club_reservas_test` con `docker compose exec db createdb -U club club_reservas_test` y, desde la raíz, correr `DATABASE_URL=postgresql://club:club@localhost:5434/club_reservas_test npx prisma migrate deploy --schema apps/api/prisma/schema.prisma`. Verificar que aplica las dos migraciones y que `ux_reserva_slot_activo` existe en `club_reservas_test`; después borrarla con `docker compose exec db dropdb -U club club_reservas_test`.
- [ ] 9.3 Reproducir el Migration Plan en limpio: `docker compose down -v` para borrar el contenedor y el volumen, y desde la raíz `npm run db:up`, `npm run db:migrate` y `npm run db:seed`. Verificar que la base queda con las tablas, los tres índices y los datos de la tarea 8.1, sin pasos manuales extra.
- [x] 9.4 Correr `npm run spec:validate` y `npm run contrato:lint`. Verificar que los dos terminan con código 0. Si el lint del contrato reporta errores en lo que ya existía, corregir solo los que no cambian la forma de la API; si alguno la cambiaría, frenar y consultar al equipo.
- [x] 9.5 Actualizar `docs/memoria-proyecto.md` con el estado final del cambio (tareas hechas, desvíos y decisiones que surjan al implementar). Verificar que su sección de estado ya no lista como pendiente nada de lo que se hizo.
- [x] 9.6 Antes del commit final, revisar `git status`: solo deben aparecer `.gitignore`, `AGENTS.md`, `docker-compose.yml`, `package.json`, `package-lock.json`, `apps/api/package.json`, `apps/api/.env.example`, `apps/api/prisma/**`, `contratos/openapi.yaml`, `docs/requisitos.md`, `docs/arquitectura.md`, `docs/identidad.md`, `docs/memoria-proyecto.md`, `docs/Consigna TP — Sistema de Reservas con OpenSpec y CI-CD.md`, `docs/claude-design/` sin `uploads/`, `openspec/config.yaml` y `openspec/changes/especificacion-base-reservas/**`, sin ningún `.env`. Verificar además que la descripción del PR enumera las precisiones que las specs agregan (sección *Risks / Trade-offs* del `design.md`), explica la ampliación del contrato y pide la aprobación de los cuatro.
