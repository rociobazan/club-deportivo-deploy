# Tasks

Referencias: los requisitos están en `openspec/specs/autenticacion/spec.md` (los siete existentes) y en `specs/autenticacion/spec.md` de este cambio (los cuatro agregados); las decisiones, en `design.md`. Los nombres de tests citan el escenario de la spec que cubren.

## 1. Rama, dependencias y verificación temprana

- [x] 1.1 Confirmar que `git branch --show-current` devuelve `feature/spec-autenticacion` y que la rama incluye `origin/main` (`git log --oneline HEAD..origin/main` vacío). Todos los commits de este cambio van ahí, en castellano y sin trailers ni pies de atribución (`AGENTS.md`).
- [x] 1.2 Instalar en el workspace `api` `@nestjs/jwt@12.0.2`, `class-validator@0.15.1` y `class-transformer@0.5.1` con `npm install --workspace api` y versiones exactas. Verificar con `npm ls --workspace api @nestjs/jwt class-validator class-transformer` que las tres figuran una sola vez.
- [x] 1.3 Escribir `apps/api/src/common/verificacion-entorno.spec.ts`: importa las tres librerías, firma y verifica un JWT con `JwtService`, hashea y compara una clave con `bcrypt`, y valida un DTO trivial con `class-validator`. Verificar que `npm run test --workspace api` pasa bajo `--experimental-vm-modules`. Si falla, frenar y decidir la alternativa de `design.md` (riesgos) antes de seguir.
- [x] 1.4 Sumar `JWT_SECRET` y `JWT_EXPIRES_IN` al bloque `env` del job `api` en `.github/workflows/ci.yml`. Verificar que `npx redocly lint` no aplica y que el YAML sigue siendo válido con `node -e "require('js-yaml')"` o, si no está, con `gh workflow view ci.yml` después del push.

## 2. Base transversal de la API

- [x] 2.1 Crear `apps/api/src/configuracion.ts` que lee `process.env` una vez, exige `JWT_SECRET` y `JWT_EXPIRES_IN` y lanza un error con mensaje claro si falta alguna (decisión 7). Verificar con un test unitario que con las variables ausentes lanza y con presentes devuelve el objeto.
- [x] 2.2 Crear `apps/api/src/prisma/prisma.service.ts` (`extends PrismaClient`, conecta en `onModuleInit`, desconecta en `onModuleDestroy`) y `prisma.module.ts` marcado `@Global()` (decisión 8). Verificar con un test e2e mínimo que la app arranca y `prisma.usuario.count()` responde contra `DATABASE_URL`.
- [x] 2.3 En `main.ts`: `setGlobalPrefix('api/v1')`, `enableCors({ origin: FRONTEND_URL, credentials: true })` y `ValidationPipe({ whitelist: true, forbidNonWhitelisted: true, transform: true })` (decisiones 3 y 9). Verificar arrancando con `npm run dev:api` que `GET /api/v1/` responde y `GET /` da 404.
- [x] 2.4 Crear `apps/api/src/common/error-de-api.ts` (`ErrorDeApi extends HttpException` con `tipo`, `titulo`, `detalle?`) y `common/filtro-de-errores.ts` (`@Catch()` global) que emite el schema `Error` del contrato con `instancia` sin el prefijo; remapea los 400 del pipe a `SOLICITUD_INVALIDA`, los 401/403 a `NO_AUTENTICADO`/`SIN_PERMISOS`, y lo no-HTTP a 500 `ERROR_INTERNO` con el detalle solo en el log (decisión 4). Verificar con tests e2e: ruta inexistente → 404 con `tipo`, `titulo`, `estado` 404 e `instancia`; `POST /auth/registro` con `{}` → 400 `SOLICITUD_INVALIDA` con `instancia` `/auth/registro` (escenario "Error de validación", adaptado porque `GET /canchas` es de 1.2); y que ningún cuerpo de error trae `statusCode` ni `message` del formato por defecto de Nest.
- [x] 2.5 Crear `common/decoradores.ts` (`@Publico()`, `@Roles(...roles)`), `common/jwt-auth.guard.ts` y `common/roles.guard.ts`, registrados con `APP_GUARD` en `app.module.ts` (decisiones 1 y 2). El guard deja `{ id, rol }` en `request.usuario`. Verificar con tests unitarios de los guards: endpoint `@Publico()` pasa sin token; sin token → 401; token vencido → 401; firma inválida → 401; rol no admitido → 403; rol admitido pasa (escenarios "Token expirado", "Token mal formado o con firma inválida", "Rol no admitido", "Rol admitido").
- [x] 2.6 Agregar en `apps/api/test/` un controlador de prueba `solo-admin.controller.ts` (`GET /prueba/solo-admin` con `@Roles('ADMIN')`), registrado únicamente en el módulo de los e2e y comentado como fixture, porque `GET /admin/panel` es de 1.6. Verificar e2e: SOCIO → 403 `SIN_PERMISOS`; ADMIN → 200; sin token → 401 `NO_AUTENTICADO` con el formato del contrato (escenario "Error de autenticación con el formato del contrato", adaptado).

## 3. Módulo `auth`

- [x] 3.1 Crear `auth/dto/registro.dto.ts` y `auth/dto/login.dto.ts` con las reglas de `RegistroRequest` y `LoginRequest` del contrato (`email` válido, `password` mínimo 8, `nombre`/`apellido` máximo 60, `telefono` opcional máximo 30). Verificar con tests unitarios del pipe que un `email` mal formado, una clave de 7 caracteres, un campo faltante y un campo extra (`rol`) fallan, y que un cuerpo válido pasa (escenarios "Datos mal formados" e "Intento de elegir el rol").
- [x] 3.2 Implementar `AuthService.registrar`: normaliza el mail a minúsculas, hashea con `bcrypt` (costo 10, como el seed), crea con rol `SOCIO` y devuelve el `Usuario` del contrato sin `passwordHash`; mail repetido → `ErrorDeApi(409, 'EMAIL_YA_REGISTRADO', …)` (decisión 5). Verificar con tests unitarios con `PrismaService` mockeado: el hash persistido es distinto de la clave y `bcrypt.compare` lo verifica; la respuesta no tiene `passwordHash` ni `password`; el 409 no llama a `create` (escenarios "Registro con un mail nuevo", "Mail ya registrado", "Contraseña hasheada al registrarse", "La contraseña no se expone").
- [x] 3.3 Implementar `AuthService.ingresar`: busca por mail normalizado, compara contra un hash fijo si no existe (tiempo constante), y ante fallo lanza el mismo `ErrorDeApi(401, 'CREDENCIALES_INVALIDAS', …)` en las dos ramas; ante éxito firma el JWT con `sub` y `rol` y calcula `expiraEn = exp - iat` (decisiones 5 y 6). Verificar con tests unitarios: mismo `tipo` y `titulo` para mail inexistente y clave incorrecta; payload con `sub` igual al id y `rol`; con `JWT_EXPIRES_IN=1h`, `expiraEn` es 3600 (escenarios "Credenciales válidas", "Vigencia informada", "Contraseña incorrecta", "Mail inexistente").
- [x] 3.4 Implementar `AuthService.perfil(id)` que devuelve el `Usuario` del contrato a partir del `sub` del token, y 401 si el usuario ya no existe. Verificar con test unitario (escenario "Consulta del propio perfil").
- [x] 3.5 Crear `auth/auth.controller.ts` (`POST /auth/registro` → 201, `POST /auth/login` → 200, `GET /auth/perfil` → 200; los dos primeros con `@Publico()`) y `auth/auth.module.ts` con `JwtModule.register({ secret, signOptions: { expiresIn } })` desde `configuracion.ts`; importar en `app.module.ts`. Verificar con `npm run build --workspace api` y `npm run lint --workspace api` en verde.
- [x] 3.6 Escribir `apps/api/test/auth.e2e-spec.ts` con un `describe` por requisito y un `it` por escenario de `openspec/specs/autenticacion/spec.md`, con el nombre del escenario, borrando `usuario` antes de cada test y fijando `JWT_SECRET` y `JWT_EXPIRES_IN` antes de crear la app (decisión 14). Verificar que `npm run test:e2e --workspace api` pasa en local contra la base de Docker y que cada escenario de la spec tiene su `it` (`grep -c "it(" ` igual al conteo de `#### Scenario:` que aplica a la API).

## 4. Front: pantallas, sesión y rutas privadas

- [x] 4.1 Leer antes de escribir (lo exige `apps/web/AGENTS.md`): `node_modules/next/dist/docs/01-app/02-guides/authentication.md` (secciones "Session Management" y "Optimistic checks with Proxy"), `03-api-reference/04-functions/cookies.md`, `01-getting-started/16-proxy.md` y `02-guides/forms.md`. Verificar anotando en el PR cualquier convención que difiera de lo que asume `design.md`.
- [x] 4.2 Crear `apps/web/lib/sesion.ts` (`import 'server-only'`): `guardarSesion(token, expiraEn)` y `borrarSesion()` con las opciones de la decisión 11, `obtenerUsuario()` que lee la cookie y llama a `GET /auth/perfil` con `apiFetch` memoizado con `cache()` y devuelve `null` ante 401 o sin cookie, y `destinoSeguro(volver)` que solo acepta rutas relativas que empiezan con `/` y no con `//` (decisión 13). Verificar con un script de Node en el scratchpad que `destinoSeguro` devuelve `/` para `https://otro.test`, `//otro.test` y `undefined`, y conserva `/mis-reservas`.
- [x] 4.3 Crear `apps/web/app/(auth)/acciones.ts` con las Server Functions `ingresar`, `registrarse` y `cerrarSesion` (decisión 10): llaman a la API, guardan o borran la cookie, y devuelven `{ error, errores }` para `useActionState` o redirigen con `redirect(destinoSeguro(volver))`. Verificar con `npm run lint --workspace web` que no hay `fetch` directo fuera de `lib/api/client.ts`.
- [x] 4.4 Crear las páginas `/ingresar` y `/registro` con `Input`, `Button` y `Card` de `components/ui`, la identidad del prototipo ("Ingresar", "Crear cuenta de socio"), link cruzado entre ambas, campo oculto `volver`, y errores por campo y generales desde `useActionState` (registro con nombre y apellido separados, decisión 14 del diseño archivado). Verificar en el navegador a 375 px y en escritorio que no hay desborde horizontal y que el foco y los `label` funcionan con teclado.
- [x] 4.5 En `app/layout.tsx`, obtener el usuario con `obtenerUsuario()` y pasarlo a `<SiteHeader usuario={…} />`; en `site-header.tsx`, mostrar "Cerrar sesión" como `<form action={cerrarSesion}>` cuando hay usuario. Verificar en el navegador que sin sesión el header muestra "Ingresar" y con sesión muestra el nombre, "Mis reservas" y "Cerrar sesión" (escenarios "Páginas del sitio accesibles desde el header" y "Usuario con sesión" de `institucional`).
- [x] 4.6 Crear `apps/web/proxy.ts` con `matcher: ['/mis-reservas', '/reservar', '/admin/:path*']`: si no hay cookie `sesion`, redirige a `/ingresar?volver=<pathname>`; si hay, deja pasar (decisión 12). Verificar con `curl -I http://localhost:3001/mis-reservas` que responde 307 con `Location: /ingresar?volver=/mis-reservas`, y que con una cookie `sesion` cualquiera responde sin redirigir.
- [x] 4.7 Verificación manual de los cuatro requisitos agregados, con los usuarios del seed, siguiendo escenario por escenario el delta `specs/autenticacion/spec.md`: ingreso válido e inválido (mismo mensaje para mail inexistente y clave incorrecta), `volver` relativo y externo, registro nuevo, repetido e inválido, cookie con `HttpOnly` visible en las DevTools y ausente en `document.cookie`, sesión vencida (`JWT_EXPIRES_IN=10s`), ruta privada con y sin sesión, y cierre de sesión. Verificar dejando en la descripción del PR la lista de escenarios con el resultado de cada uno.
- [x] 4.8 `npm run lint --workspace web` y `npm run build --workspace web` en verde, borrando antes `apps/web/.next` para no arrastrar caché de otra rama.

## 5. Documentación, revisión y PR

- [x] 5.1 En `README.md`: indicar que `JWT_SECRET` y `JWT_EXPIRES_IN` son obligatorias para arrancar la API (con un valor de ejemplo para desarrollo) y cómo probar el ingreso con `socio@club.test` / `clave1234`. Verificar leyendo la sección de ejecución de punta a punta.
- [x] 5.2 En `docs/memoria-proyecto.md`: decisión nueva con lo que este cambio fija para las demás features (guards globales con `@Publico()`/`@Roles()`, `ErrorDeApi`, `PrismaModule`, cookie `sesion` y `obtenerUsuario()`), más la línea del historial. En `docs/estado-del-proyecto.md`: 1.1 pasa a hecho, se actualiza el resumen y se anota que 1.3 y 1.4 ya pueden proteger sus endpoints. Verificar que los dos documentos mencionan el mismo número de decisión.
- [x] 5.3 Correr `npm run spec:validate`, `npm run test --workspace api`, `npm run test:e2e --workspace api`, y `/code-review` sobre el diff completo; corregir lo que salga. Verificar que los cuatro terminan sin hallazgos ni errores.
- [ ] 5.4 Abrir el PR con `/commit-commands:commit-push-pr` desde `feature/spec-autenticacion` hacia `main`, con la lista de escenarios verificados (4.7) y la pregunta abierta de `design.md` (Server Functions vs Route Handler) para que el equipo la cierre en la revisión. Verificar que el CI muestra `specs`, `api` y `web` en verde y que el PR queda bloqueado esperando la aprobación, que es lo que exige la protección.
