# Tasks

Referencias: requisitos de la API en `openspec/specs/catalogo/spec.md` (los tres públicos) y `openspec/specs/disponibilidad/spec.md`; requisitos de pantalla en `specs/catalogo/spec.md` y `specs/disponibilidad/spec.md` de este cambio; decisiones en `design.md`. Los nombres de tests citan el escenario que cubren.

## 1. Rama, configuración y reloj

- [x] 1.1 Confirmar que `git branch --show-current` devuelve `feature/spec-disponibilidad` y que incluye el último commit de `feature/spec-autenticacion` (`git log --oneline feature/spec-autenticacion..HEAD` no muestra faltantes al revés). Commits en castellano, sin trailers de atribución (`AGENTS.md`).
- [x] 1.2 En `apps/api/src/configuracion.ts`, agregar `horaApertura`, `horaCierre` y `zonaHoraria` desde `HORA_APERTURA`, `HORA_CIERRE` y `ZONA_HORARIA_CLUB` con los defaults de `requisitos.md` §4 (`08:00`, `23:00`, `America/Argentina/Cordoba`), validando formato `HH:MM`, apertura anterior al cierre y zona horaria existente (decisión 8). Verificar con unitarios en `configuracion.spec.ts`: defaults sin variables; `HORA_CIERRE=7pm` corta; `HORA_APERTURA=23:00` con cierre `08:00` corta; `ZONA_HORARIA_CLUB=Marte/Base` corta.
- [x] 1.3 Crear `apps/api/src/common/reloj.ts` (`Reloj` inyectable con `ahora(): { fecha, hora }` en la zona del club vía `Intl.DateTimeFormat`, decisión 1) y registrarlo como provider global en `AppModule`. Verificar con `reloj.spec.ts`: con `Date` fijo en `2026-09-15T17:30:00Z` y zona `America/Argentina/Cordoba` devuelve `{ fecha: '2026-09-15', hora: '14:30' }`, y con zona `UTC` devuelve `17:30`.
- [x] 1.4 Crear `apps/api/src/common/grilla.ts` con `generarGrilla(apertura, cierre, duracionMin)` pura (decisión 2). Verificar con `grilla.spec.ts` los escenarios "Día sin reservas en una cancha de tenis" (15 turnos, 08:00–09:00 a 22:00–23:00), "Día sin reservas en una cancha de pádel" (10 turnos, último 21:30–23:00), "Cierre anticipado" (tenis con cierre 22:00: último 21:00–22:00) y "Bloque que no entra antes del cierre" (pádel con cierre 22:00: último 20:00–21:30).
- [x] 1.5 Crear `apps/api/src/common/fechas.ts` con `esFechaValida('YYYY-MM-DD')` (formato y existencia real, sin `2026-02-30`), `esHoraValida('HH:MM')` y `compararFechas`. Verificar con `fechas.spec.ts`: `15-09-2026`, `2026-02-30` y `8pm` se rechazan; `2026-09-15` y `20:00` se aceptan.

## 2. Guard: identificar al usuario en endpoints públicos

- [x] 2.1 Cambiar `apps/api/src/common/jwt-auth.guard.ts` para que en un endpoint `@Publico()` verifique el `Authorization` si viene y deje `request.usuario` cuando es válido, y siga como anónimo (sin lanzar) cuando falta o es inválido (decisión 3). Verificar con dos unitarios nuevos en `jwt-auth.guard.spec.ts`: público con token válido deja `{ id, rol }`; público con token inválido pasa sin usuario. Los tests existentes de endpoints protegidos siguen en verde.
- [x] 2.2 Agregar a `common/usuario-actual.ts` un helper `exigirRol(solicitud, rol)` que lanza `noAutenticado()` sin usuario y `ErrorDeApi(403, 'SIN_PERMISOS', …)` con otro rol, para que los controladores públicos con variante privada lo llamen. Verificar con unitarios: sin usuario → 401; `SOCIO` pidiendo `ADMIN` → 403; `ADMIN` pasa.

## 3. Módulo `catalogo`

- [x] 3.1 Crear `apps/api/src/catalogo/dto/` con los DTOs de query de canchas (`disciplinaId` entero ≥ 1, `techada` booleano, `incluirInactivas` booleano) y de equipamiento (`disciplinaId`, `fecha`, `horaInicio` con el patrón del contrato, `incluirInactivos`), con `@Type` y `@Transform` explícitos (decisión 6). Verificar con unitarios del pipe: `disciplinaId=abc` → rechaza; `techada=si` → rechaza; `techada=true` → `true` booleano; `horaInicio=8pm` → rechaza (escenario "Hora mal formada").
- [x] 3.2 Crear `catalogo/mapeadores.ts` con `aDisciplina()`, `aCancha()` (con `disciplina` desnormalizada) y `aEquipamiento()` que convierten `Prisma.Decimal` a `number` (decisión 7). Verificar con unitario que `JSON.stringify(aCancha(...))` produce `"precioPorTurno":9000` numérico y no `"9000"`.
- [x] 3.3 Implementar `CatalogoService.disciplinas()` (activas, ordenadas por id) y `listarCanchas(filtros, usuario?)`: activas salvo `incluirInactivas=true`, que exige `ADMIN` vía `exigirRol`; filtros `disciplinaId` y `techada`; una sola consulta con `include` de disciplina. Verificar con unitarios con Prisma mockeado: `incluirInactivas` sin usuario → 401 y no consulta; con `SOCIO` → 403; con `ADMIN` → consulta sin filtro `activa`.
- [x] 3.4 Implementar `CatalogoService.listarEquipamiento(filtros, usuario?)`: activo salvo `incluirInactivos` (mismo control), filtro `disciplinaId`; `fecha` y `horaInicio` juntos o `ErrorDeApi(400, 'SOLICITUD_INVALIDA', …)`; con ambos, `stockDisponible = max(0, stockTotal − alquilado)` calculado con un único `groupBy` sobre `reservaEquipamiento` filtrado por la reserva (decisión 4). Verificar con unitarios: sin fecha no hay `stockDisponible`; con alquileres 2 sobre stock 6 → 4; alquileres mayores al stock → 0; `fecha` sin `horaInicio` → 400 (escenario "Fecha sin hora de inicio").
- [x] 3.5 Crear `catalogo/catalogo.controller.ts` (`GET /disciplinas`, `GET /canchas`, `GET /equipamiento`, los tres `@Publico()`) y `catalogo.module.ts`, e importarlo en `AppModule`. Verificar `npm run build --workspace api` y `npm run lint --workspace api` en verde.
- [x] 3.6 Escribir `apps/api/test/catalogo.e2e-spec.ts` con un `describe` por requisito público y un `it` por escenario: "Tres disciplinas activas" y "Duración de turno informada" (contra el seed), "Disciplina inactiva", "Filtro por disciplina", "Disciplina inexistente", "Filtro por canchas techadas", "Precio plano por cancha" (tipo `number`), "Cancha inactiva", "Catálogo sin fecha", "Stock disponible en un turno con alquileres", "Stock compartido entre canchas de la misma disciplina", "Alquiler de una reserva cancelada", "Fecha sin hora de inicio", "Hora mal formada", más `incluirInactivas` sin token → 401, con `SOCIO` → 403 y con `ADMIN` → incluye la inactiva. Los datos propios se crean con Prisma con nombres `e2e-…` y un usuario `@e2e.test`, y se borran solo esos al terminar. Verificar `npm run test:e2e --workspace api` en verde y que cada escenario de la spec tiene su `it`.

## 4. Módulo `disponibilidad`

- [ ] 4.1 Crear `disponibilidad/dto/consulta-disponibilidad.dto.ts` (`fecha` obligatoria y válida, `disciplinaId` y `canchaId` enteros ≥ 1). Verificar con unitarios del pipe: sin `fecha` → rechaza; `fecha=15-09-2026` → rechaza (escenario "Fecha ausente o mal formada").
- [ ] 4.2 Implementar `DisponibilidadService.consultar(filtros)` (decisión 5): rechaza fecha anterior a `reloj.ahora().fecha` con 400 `SOLICITUD_INVALIDA`; trae canchas activas de disciplinas activas con la duración de su disciplina; trae en una segunda consulta las reservas no canceladas de esa fecha para esas canchas; por cancha, `generarGrilla` menos las `horaInicio` ocupadas y, si es hoy, menos los bloques con inicio anterior a la hora actual. Devuelve `DisponibilidadResponse` con `precioPorTurno` numérico. Verificar con unitarios con Prisma y `Reloj` mockeados: fecha de ayer → 400; hoy a las 14:00 → primer turno 14:00–15:00 (escenario "Consulta de hoy a media tarde"); una reserva a las 19:00 quita solo ese bloque de esa cancha.
- [ ] 4.3 Crear `disponibilidad.controller.ts` (`GET /disponibilidad`, `@Publico()`) y `disponibilidad.module.ts`, importarlo en `AppModule`. Verificar build y lint en verde.
- [ ] 4.4 Escribir `apps/api/test/disponibilidad.e2e-spec.ts` con un `it` por escenario: "Día sin reservas en una cancha de tenis", "Día sin reservas en una cancha de pádel", "Turno ocupado", "Turno liberado por cancelación", "Reserva en otra cancha", "Cierre anticipado" y "Bloque que no entra antes del cierre" (app aparte con `HORA_CIERRE=22:00`), "Filtro por disciplina", "Filtro por cancha", "Fecha ausente o mal formada", "Fecha anterior a hoy" y "Consulta de hoy a media tarde" (con `Reloj` reemplazado por `overrideProvider`). Datos propios `e2e-…` creados y borrados por el test. Verificar `npm run test:e2e --workspace api` en verde y el conteo de `it` contra los `#### Scenario:` de la spec.

## 5. Front: Canchas y precios, y Disponibilidad

- [ ] 5.1 Leer antes de escribir (`apps/web/AGENTS.md`): en `node_modules/next/dist/docs/01-app/`, `01-getting-started/` sobre `searchParams` asíncronos y `Link`, y `03-api-reference/02-components/form.md`. Verificar anotando en el PR cualquier convención que difiera del `design.md`.
- [ ] 5.2 Crear `apps/web/lib/club.ts` (hoy en la zona del club con `Intl`, y `HORA_APERTURA`/`HORA_CIERRE`/`ZONA_HORARIA_CLUB` desde el entorno con los mismos defaults que la API, decisión 9) y `apps/web/lib/grilla.ts` (copia pura de `generarGrilla`, decisión 2), y sumar las tres variables comentadas a `apps/web/.env.example`. Verificar con un script de Node en el scratchpad que `generarGrilla('08:00','23:00',90)` da 10 bloques con el último `21:30–23:00`, y que `hoyEnElClub()` coincide con la API para un mismo instante.
- [ ] 5.3 Crear `apps/web/app/canchas/page.tsx` (Server Component): `Promise.all` de `GET /disciplinas`, `GET /canchas` y `GET /equipamiento` con `timeoutMs: 2000`, agrupado por disciplina, con superficie, techada, precio, duración del turno, equipamiento con stock total, los dos textos del prototipo y un `<Link>` "Ver disponibilidad" a `/disponibilidad?disciplinaId=` por disciplina; ante `ApiHttpError`, el componente `EstadoError` con "Reintentar" (decisión 11). Verificar en el navegador a 375 px y escritorio con el seed: tres disciplinas, precios sin decimales raros, sin desborde, y con la API apagada se ve el aviso con el header y el pie.
- [ ] 5.4 Crear `apps/web/app/disponibilidad/page.tsx` (Server Component): lee `fecha` y `disciplinaId` de `searchParams`; fecha pasada o inválida → consulta hoy con aviso; `<form method="get">` con `<input type="date" min={hoy}>` y `<select>` de disciplinas; llama a `GET /disponibilidad`; por cancha, la grilla completa marcando Libre / Ocupada / Pasado (decisión 9). Verificar en el navegador: hoy por defecto; cambiar a Pádel muestra bloques de 90 minutos; con una reserva creada a mano en la base, ese bloque se ve Ocupada; a 375 px sin desborde.
- [ ] 5.5 Crear `apps/web/app/disponibilidad/aviso-socio.tsx` (Client Component con `<dialog>`, decisión 10) y la grilla: sin sesión, tocar un bloque Libre abre el aviso con links a `/ingresar?volver=` y `/registro?volver=` con la consulta actual; con sesión, los bloques Libre son `<Link>` a `/reservar?canchaId=&fecha=&horaInicio=`. Verificar en el navegador los escenarios "Visitante elige un turno libre" (aviso, e ingresar vuelve a la misma consulta) y "Socio elige un turno libre" (URL de `/reservar` con los tres parámetros).
- [ ] 5.6 Verificación manual de los dos requisitos de pantalla, escenario por escenario, dejando la lista con el resultado en la descripción del PR; después `npm run lint --workspace web` y `npm run build --workspace web` en verde (borrando `apps/web/.next` antes).

## 6. Documentación, revisión y PR

- [ ] 6.1 README: las tres variables con sus defaults en los dos `.env.example`, y que la disponibilidad se ve en `/disponibilidad` y el catálogo en `/canchas`. Verificar leyendo la sección de ejecución.
- [ ] 6.2 `docs/memoria-proyecto.md`: decisión nueva con lo que este cambio deja fijado (`Reloj`, grilla pura, mapeadores Decimal→number, guard que identifica en públicos, `exigirRol`) y la línea del historial; `docs/estado-del-proyecto.md`: 1.2 en el PR, operaciones 7 de 18, pantallas del sitio. Verificar que los dos documentos mencionan el mismo número de decisión.
- [ ] 6.3 Correr `npm run spec:validate`, `npm run test --workspace api`, `npm run test:e2e --workspace api`, lint y build de `web`, y `/code-review` sobre el diff contra `feature/spec-autenticacion`; corregir lo que salga. Verificar que todo termina sin errores ni hallazgos abiertos.
- [ ] 6.4 Abrir el PR desde `feature/spec-disponibilidad` **hacia `feature/spec-autenticacion`** con `/commit-commands:commit-push-pr`, con la lista de escenarios verificados y la nota de que se retargetea a `main` cuando entre el #19 (retargetear, nunca mergear el de arriba en la rama base). Verificar que el CI muestra `specs`, `api` y `web` en verde.
